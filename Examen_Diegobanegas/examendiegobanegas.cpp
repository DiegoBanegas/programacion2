#include <iostream>
#include <stdio.h>
#include <stdlib.h>
using namespace std;

int main()
{   int numero,suma,mayor,cnumero=0;
    double promedio;
    char resp;

   do
    {
        do
        {
            cout<<"Ingresar el numero";
            cin>>numero;

          }while (((numero%2)!=0) or ((numero>=1)=<200));

                do
                {
                    }while ((resp='S') and (resp='N'));
                    cout<<"Desea continuar....:";
                    cin.get(resp);
                    _flushall();
                do
                {
                    if (numero>mayor)
                   {
                      numero=mayor;
                   }

                   if ()
while
            if ((numero%2)==0)
            {
                cnumero++;
            }

                   if(numero<200)
                   {
                       cnumero++;
                       suma+=numero;
                   }
    if (cnumero!=0)
                   {
                       promedio=suma/cnumero;
                   }


while (resp!='N');

    cout<<"La suma de digitos es....:"<<cifrat<<"\n";
    cout<<"Numero mayor es.....:"<<mayor<<"\n";
    cout<<"Promedio es.....:"<<promedio<<"\n";
    return 0;
}
